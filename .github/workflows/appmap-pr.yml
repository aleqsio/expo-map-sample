# .github/workflows/appmap-pr.yml — review what a PR changes on-screen
name: appmap · PR changes
on:
  pull_request:
    types: [opened, synchronize, reopened]
concurrency:
  group: appmap-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true
permissions:
  contents: write        # push bundles to the appmaps branch (set publish: false to drop this)
  pull-requests: write   # sticky comment
jobs:
  changes:
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: aleqsio/expo-map@main
        with:
          mode: pr
          agent_api_key: ${{ secrets.AGENT_API_KEY }}   # opencode key — omit for deterministic-only
          agent_provider: opencode
          xcode: "26.2"   # 26.3 trips SDK 57 from-source strictness, 16.4 lacks Swift 6.2
          # project: apps/mobile
          # publish: "false"   # private repos that only want the artifact link
