# .github/workflows/appmap-baseline.yml — keep the map of main fresh, and
# turn agent discoveries into committed flows
name: appmap · baseline
on:
  push:
    branches: [main]
  schedule:
    - cron: "0 5 * * 1-5"   # weekday mornings, in case main was quiet
  workflow_dispatch:
    inputs:
      full:
        description: Rebuild the whole map instead of incrementally
        type: boolean
        default: false
concurrency:
  group: appmap-baseline
  cancel-in-progress: false
permissions:
  contents: write
  pull-requests: write
jobs:
  baseline:
    runs-on: macos-26
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: aleqsio/expo-map@main
        with:
          mode: baseline
          agent_api_key: ${{ secrets.AGENT_API_KEY }}
          agent_provider: opencode
          flows_pr: "true"
